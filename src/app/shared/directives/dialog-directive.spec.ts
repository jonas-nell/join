import { Dialog } from '../../dialog-directive';

describe('Dialog', () => {
  it('should create an instance', () => {
    const directive = new Dialog();
    expect(directive).toBeTruthy();
  });
});
